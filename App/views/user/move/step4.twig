{% extends 'macros/link.twig' %}
{% block link_content %}
    {% if error %}
            <div class="alert alert-error">
                {{ error }}
                <i class="icon icon-remove"></i>
            </div>
    {% endif %}
    <h5>Veuillez taper le code reçus en jeu :</h5>
    <br>
    <form method="post" action="/move" class="recharge-chose-custom">
        <input type="hidden" name="step" value="4">

        <label for="code">Code :</label>
        <input name="code" class="u-full-width" type="text" placeholder="FLUORLEBG"
               id="code" required>
        <div class="c-input-group">
            <button class="button button-danger" type="submit">Valider</button>
        </div>
    </form>

    <div class="shop-summary">
        <div class="shop-summary-icon">
            <i class="icon-info-sign icon"></i> Si vous ne recevez pas de message merci de contacter un administrateur.
        </div>
    </div>
{% endblock %}

